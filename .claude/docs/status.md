# Capability Status Map

What actually works vs. what is a stub. **Check this before touching a subsystem** — the
biggest trap in this codebase is features whose **UI is fully built but backend is missing**.
Treat each row as the current contract; verify against code, and update this file when you
ship a change that moves a row.

## ✅ Complete (backend + UI, production-ready)

| Capability | Notes / key classes |
|------------|---------------------|
| Dynamic schema engine | Full field-type set (primitives, date/time, JSON/array, picklists, currency, percent, auto-number, phone/email/URL, rich text, encrypted, external ID, geolocation, relationship — see `FieldType` enum); LOOKUP (FK SET NULL) + MASTER_DETAIL (FK CASCADE); FORMULA + ROLLUP_SUMMARY computed fields; auto-number sequences; schema snapshots |
| Auto-generated JSON:API | Full CRUD per collection, includes (multi-level + Redis cache), sparse fieldsets, filter/sort/paginate, sub-resources, atomic ops `POST /{tenant}/_operations`, dynamic route registration via NATS |
| Global full-text search | Postgres `tsvector`, `GET /api/_search?q=`, per-field `searchable`, RLS-isolated, NATS-driven index maintenance |
| Auth / identity | Internal OIDC provider (`kelta-auth`, Spring Authorization Server); federated brokering (`FederatedUserMapper`); dynamic JWKS by issuer (Redis 15-min TTL); JDBC sessions; password reset/forced-change |
| MFA | TOTP (RFC 6238) + recovery codes + SMS OTP; encrypted secrets; rate-limited; `MfaController` `/api/auth/mfa/*`; `SmsProvider` SPI |
| Password policies | Per-tenant length/complexity + account lockout |
| Personal Access Tokens | `klt_` + 40 chars, SHA-256 hashed; `/api/me/tokens` (max 10); Redis revocation (`pat:revoked:`) |
| RBAC | Profiles + permission sets + groups; most-permissive-wins; per-profile ABAC CEL rules; 7 built-in profiles |
| Cerbos authz | Per-tenant policy generation/sync; gateway `RouteAuthorizationFilter` (route) + worker `CerbosRecordAuthorizationAdvice` (record); fail-closed circuit breaker (3 fails → open 10s) |
| **Field-Level Security (read + write)** | Write: `CerbosFieldWriteSecurityAdvice` blocks HIDDEN on create/update. **Read: `CerbosFieldSecurityAdvice` strips denied `attributes` + to-one `relationships`** (shipped #1038/627d4ca9). System audit fields never stripped |
| Data security | AES-256-GCM field encryption (per-tenant key derivation); Postgres RLS on all system tables; per-tenant schema isolation; OpenSearch audit trails |
| Multi-tenancy & governors | URL slug resolution + custom domains (`CustomDomainFilter`); per-tenant governor limits (API/day, storage, users, collections, fields, workflows, reports) with live usage |
| Visual Flow Builder | 8 state types, 16 action handlers, JSONPath data flow, durable execution (survives restart), retry/catch, versioning, React Flow designer. **Triggers working: record / API-webhook / scheduled-cron.** Adding a handler also needs UI wiring (hardcoded `RESOURCE_GROUPS` + `*Params.tsx`; the `ActionHandlerDescriptor` SPI is **not** consumed by the builder) — see `playbooks.md`. (TODOs below) |
| MCP server (admin + user toolsets) | Built. kelta-admin (schema/field/flow/layout/picklist/validation-rule tools) + kelta-user (records/flows/approvals/search). **Many tools already exist — check `kelta-mcp/.../tool/{admin,user}/` before adding one** (e.g. `create_validation_rule` and the `validation-rules` system collection are already implemented). |
| Rate limiting | Per-tenant sliding window (Redis) + daily governor quota + IP-based; fail-open if Redis down |
| Observability | OTel → Jaeger → OpenSearch tracing; spans tagged with `kelta.tenant.id`/`user.id`/`correlation.id` (`SpanBodyEnrichmentFilter`); `OpenSearchLogAppender`; metrics dashboard; audit; Prometheus `kelta_worker_*` metrics; 7 admin monitoring pages; per-tenant retention; **flow-run observability** (`FlowExecutionController` — list executions + status/duration/error + step detail). **Per-tenant OTLP export (Rec 7)**: `PerTenantOtlpSpanProcessor` (additive — never disturbs the default pipeline) forwards each span to its tenant's own OTLP collector based on `kelta.tenant.id`, via `TenantOtlpRegistry` + a per-tenant batching `OtlpHttpSpanExporter` cached by tenant; targets from a **self-service** source — `DbTenantOtlpRegistry` (`@Primary`) reads `tenant_otlp_target` (V145, RLS; Caffeine 60s TTL) and falls back to operator config (`kelta.observability.tenant-otlp.targets.<tenantId>.*`); a tenant sets its own collector via `GET/PUT/DELETE /api/admin/observability/otlp-target` (`VIEW_SETUP`-gated), which upserts + invalidates the cache. Known edge: the processor caches the exporter per tenant, so re-pointing an already-active tenant's endpoint takes effect after a restart (keying the exporter cache by endpoint = small follow-up) |
| Integration | HTTP callout, outbound message, NATS publish, inbound webhooks (`POST /api/webhooks/{flowId}`), Svix outbound, SMTP email, `PushProvider` SPI, Superset analytics, merge-field templating |
| Embedded analytics (Superset) | Guest tokens, dashboard discovery, dataset sync (NATS-triggered), per-tenant isolation, `SupersetEmbed` |
| WebSocket realtime | `/ws/realtime` (JWT on upgrade), `RealtimeBridge` ← `kelta.record.changed`, per-session subscription limits, tenant-scoped |
| Image transforms | `GET /api/images/{path}?w=&h=&fit=&format=&quality=`; resize/crop/convert; bomb protection |
| Direct file serving | `GET /api/files/{path}` with range support, tenant-scoped |
| Notes & attachments | Full S3 lifecycle: presigned-PUT upload + finalize, list/download (`AttachmentUrlEnricher`), delete with S3 cleanup, `AttachmentCleanupHook` cascade on parent delete (idx `idx_attachment_tenant_record`, V142) |
| Metadata dependency & impact | `MetadataDependencyService` per-tenant graph; `GET /api/metadata/impact?type=&id=&direction=` and `/api/metadata/graph`; Tarjan SCC cycle detection (#1039). **Deployment plans (Rec 5)**: `POST /api/metadata/deployment-plan` `{changeSet:[{type,id}], includeDependencies}` → dependency-ordered apply sequence (Kahn topo-sort, deps first), with `missing`/`cyclic` items flagged |
| Config-health governance (Rec 6) | `ConfigHealthAnalyzer` scans tenant config via pluggable `HealthRule` beans (circular master-detail, collections without layouts/fields, orphan fields, flows without error handling, over-permissive profiles) → 0–100 score + findings; `GET /api/config-health`. **Dashboard UI**: `ConfigHealthPage` at `monitoring/health` (Monitoring hub tab, `VIEW_SETUP`-gated) — score gauge + per-severity counts + findings list sorted error→warning→info, with re-scan; 4 Vitest. AiProposal one-click remediation still pending; positive Playwright e2e post-deploy |
| Developer experience | `@kelta/sdk` (`KeltaClient`, `QueryBuilder`) + **OpenAPI→TS type generator** (`@kelta/sdk/cli`: `openapi-parser` + `type-generator`, `kelta-generate-types` bin / `generateTypesFromSpec`); `@kelta/cli` — auth/collections/records + **`metadata export`/`diff`/`apply`** (Rec 10: GitOps for tenant config over the Package API — `POST /api/packages/{export,import/preview,import}`; `apply --dry-run`) + **`sdk types`** (Rec 10: authenticated, tenant-aware codegen — fetches `GET /api/docs/openapi.json` and delegates to the `@kelta/sdk` generator, so no OpenAPI URL/token needs passing once logged in); OpenAPI 3.0 + Swagger at `/api/docs`, Zod schemas, i18n (6 langs, RTL), theme, a11y, 70+ UI pages |
| Operational | Graceful shutdown (persist in-flight flows), optimistic locking for scheduled tasks, multi-layer cache (Caffeine→Redis→worker) w/ NATS invalidation, security headers, health endpoints |

## 🟡 Partial — backend exists, gaps remain

| Capability | Works | Missing |
|------------|-------|---------|
| Visual Flow triggers | record / API-webhook / scheduled-cron | **NATS_TRIGGERED**: no dynamic per-flow consumer. **Wait-state resume**: `resumeExecution()` unimplemented — long Waits persist WAITING but never resume |
| Extensibility / modules | `KeltaModule` SPI, 3 compile-time modules, per-tenant lifecycle w/ NATS propagation, `@kelta/plugin-sdk`. **Runtime JAR loading is real** (not a placeholder): `ModuleJarService` (S3 upload/download + local cache + SHA-256 checksum), `SandboxedModuleClassLoader` (child-first `URLClassLoader` with a platform-package allowlist — module classes can't reach disallowed platform internals), `RuntimeModuleManager` (install/enable/disable/uninstall + per-tenant load via the sandbox classloader; `installModuleWithJar` → S3 + `tenant_module` record). **JAR signature verification (Rec 9)**: `ModuleSignatureVerifier` checks a detached publisher signature over the JAR bytes against a trusted X.509/SPKI public key (`kelta.modules.signing.public-key`, PEM; Ed25519 default, SHA256withRSA/ECDSA supported) **before** any S3 upload or DB write; enforced when a key is configured (unset → no-op, logged), so only publisher-signed modules install. `POST /api/modules/install-jar` takes an optional `signature` part → 403 on failure | Re-verify the downloaded JAR signature on load (defense-in-depth vs S3 tamper; install-time gate ships now); admin UI doesn't yet query `ComponentRegistry` to render plugin components; "Kelta Exchange" marketplace catalog (browse/install listings) not built |
| Integration | (see Complete) | **Script execution**: `ScriptExecutor` SPI is a no-op (no GraalVM/JS engine). Connected Apps: client-credentials works, full auth-code flow not |
| Scheduled jobs | Flow-type cron triggers work | General `scheduled_jobs` dispatcher for SCRIPT / REPORT_EXPORT job types not built |
| SAML 2.0 SSO (Rec 8) | **Config foundation**: `saml-providers` system collection (`saml_provider`, V146, RLS) — per-tenant IdP config (registrationId, idpEntityId, ssoUrl, idpCertificate PEM, nameIdFormat, email/profile attribute mappings, active); managed via the generic collection CRUD/MCP, mirroring `oidc-providers`. **SSO flow (kelta-auth)**: `spring-security-saml2-service-provider` + OpenSAML 5.1.6 (Shibboleth Maven repo — not on Central). `DynamicRelyingPartyRegistrationRepository` loads per-tenant config via `WorkerClient` and builds a `RelyingPartyRegistration` per `{tenantId}:{providerId}` (IdP verification cert parsed from the stored PEM, 5-min cache) — the SAML mirror of `DynamicClientRegistrationRepository`. `@Order(0)` SecurityFilterChain on `/saml2/**` + `/login/saml2/**` (`saml2Login` + `saml2Metadata`); validated assertion → `FederatedUserMapper.mapSamlUser` (shares the JIT-provision tail with OIDC; email from the provider's `emailAttribute`→common attrs→NameID, profile via `profileAttribute`→`findProfileByName`, else `Minimum Access` fallback). **Platform SP request signing** (`kelta.auth.saml.sp-signing-{certificate,private-key}` PEM, env-injected): one platform-wide SP keypair signs outbound AuthnRequests and is published via `/saml2/metadata/{registrationId}`; absent → requests sent unsigned. Login page renders SAML buttons → `/saml2/authenticate/{registrationId}`. Worker `/internal/saml/{providers,by-entity-id}` endpoints serve the config (FORCE-RLS, read via admin-bypass + `tenant_id` filter). The whole SAML chain + repo are gated on `FederatedUserMapper` (i.e. `EncryptionService` configured), mirroring OIDC. Tests: 8 registration-repo + 5 SAML-mapper + 2 login-button + 4 worker-endpoint + harness scenario exercises the internal endpoint on real Postgres | Positive Playwright e2e needs a live deployed IdP (post-deploy, like other new admin flows); IdP-initiated SSO + Single Logout not wired; SP metadata advertises a signing cert only when the SP keypair is configured. **Security — not auto-merged.** |
| Page builder / screen builder | `PageBuilderPage` editor + CRUD; `ui-pages` has `slug`/`published`/`config` (V116); **render contract**: `GET /api/pages/{slug}/render` (`PageRenderController`/`PageRenderService`) serves a published+active page as a **versioned** `PageRenderContract` (`{version, slug, title, path, variables, dataSources, tree}` — bumped to **v2** in slice 1g: `tree` is the whole `config` map verbatim and page-level `variables`/`dataSources` are surfaced as siblings, empty for legacy pages, as a pure pass-through that resolves no bindings/dataSources server-side, preserving Cerbos/FLS; consumed by the binding/logic slices 2d/2e); **widget descriptor registry + shared `RenderTree`** (slice 2a): the runtime `PageTreeRenderer` renders through one registry-driven path (`PageBuilderPage/widgets/{registry,renderTree}`) instead of a per-type switch — built-ins register as descriptors under `widgets/builtins`, plugin page components fall back via `componentRegistry`, gated by `RENDER_TREE_V2` with the legacy switch kept as a fallback; gateway static route `/api/pages/**` registered (Rec 1 slice 1a); **builder persists the component tree into the `config` JSON column** (slice 1b — previously `components`/`layout` were sent as top-level attributes and silently dropped by the worker, so saved pages lost their tree); **end-user renderer**: `CustomPage` (route `/:tenant/app/p/:slug`) fetches the render contract and renders the tree via the schema-driven `PageTreeRenderer` (heading/text/button/image/card/container/table/form → React, plugin components via `componentRegistry`) — slice 1c; **`UIPageConfigEventPublisher` broadcasts `kelta.config.page.changed.<pageId>`** on page create/update/delete for multi-pod cache invalidation (slice 1d); **table data binding**: a `table` node with `props.dataView = {collection, fields, limit}` renders live rows — `DataTableNode` fetches `/api/{collection}` via the authorized JSON:API path (gateway+worker enforce Cerbos/FLS; denied fields stripped server-side) and shows the declared columns (slice 1e-render); **builder PropertyPanel data-source editor** for table+form (collection/fields/limit → `props.dataView`, slice 1e-ui); **form create binding**: a `form` node renders an input per declared field and submits a new record via `apiClient.postResource('/api/{collection}', …)` — the authorized JSON:API create path (Cerbos/FLS enforced) — slice 1f; **page create auto-derives the required `slug`**: `UIPageSlugHook` (before-save on `ui-pages`) slugifies `name` (→`path`→`title`) and disambiguates per tenant when absent — the `ui_page.slug` column is `NOT NULL`/`UNIQUE` (V116) but builder/API/MCP payloads don't send it, so creates 500'd before this fix; **schema-driven inspector + palette (slice 2b)**: the palette (`PageBuilderPage/palette/Palette.tsx`) renders from `widgetRegistry.listByCategory()` grouped by category, and the inspector (`PageBuilderPage/inspector/Inspector.tsx`) loops over `descriptor.propSchema`, mapping each `PropFieldKind` to a field editor under `inspector/fields/*` — the hardcoded `AVAILABLE_COMPONENTS` array, `ComponentPalette`, per-type `PropertyPanel` blocks, and the `Canvas`/`Preview` per-type previews are deleted (the canvas node chip + preview now render through the descriptor/`RenderTree`). `bindable` props get an `fx` literal↔expression toggle (`BindableField`) that writes `{ $bind, mode:'expr' }` (the picker stores the bare token; `{{…}}` is display-only); `EventListField` authors `PageAction[]` into `node.events` (add/remove/reorder, tabs over `descriptor.supportedEvents`). **Binding resolution lands in 2d; the action runtime in 2e (both shipped)**; **layout engine + `@dnd-kit` canvas (slice 2c)**: new `grid`/`row`/`column`/`divider` layout widgets; per-child responsive `span {base,sm,md,lg}` → Tailwind `col-span-*` + breakpoint prefixes (`PageBuilderPage/canvas/spanClasses.ts`, 48 literal classes for the JIT scanner); the native-HTML5 stack-only canvas is replaced by `canvas/Canvas.tsx` — one `DndContext` (Pointer + Keyboard sensors, `closestCenter`) hosting the palette draggables + per-container `SortableContext` reorder, **drop-into-container** (no longer appends to root — routes through `model/treeOps.ts` `insertNode`/`moveNode`), and **grid-span resize** snapping to the 12-col grid (`canvas/dnd/useSpanResize.ts`, only `span` persisted — no px/row/column); `model/treeOps.ts` (pure `insert`/`move`/`remove`/`updateProps`/`setSpan` with no-op + cycle guards) and `model/migrate.ts` convert a legacy `position {row,column,width,height}` tree to `grid`/`column`+`span` on builder load (idempotent, persisted on next save). **`handleSavePage`/`mergeConfig` widened to stamp `schemaVersion: 2` and round-trip `variables`/`dataSources`/`access` (the page-level siblings persist only if the save call passes them)**; unsaved-changes confirm + `beforeunload` guard added; `config.layout` (create-form `layoutType` select) is now inert legacy (removal deferred). `@dnd-kit/{core,sortable,utilities}` added to `kelta-ui/app`, scoped to the page canvas only; **data binding & expressions (slice 2d)**: any prop may be a binding `{ $bind, mode? }` resolved **client-side** (`model/resolveBindings.ts` over `@kelta/formula`; `path` mode walks a dotted/`[n]` path, `expr` mode flattens scope leaves then `evaluate`s; `{{…}}` interpolation; `getPath` refuses `__proto__`/`constructor`/`prototype`); page-level **variables** (`usePageVariables`, defaults seeded) + on-load **dataSources** (`usePageDataSources` fetches over the authorized JSON:API path — caps `MAX_PAGE_DATA_SOURCES=12`/`MAX_REPEATER_ROWS=200` in `model/limits.ts`); a **page-settings drawer** (`inspector/pageSettings/*`) authors variables/data sources (persisted via the 2c save path); new `field-value`/`list`/`repeater` widgets (the repeater re-resolves children per `item` row); `CustomPage` builds the runtime `BindingScope` (vars+data+page) and threads it into `RenderTree`; **events & actions (slice 2e)**: page widgets fire an ordered `PageAction[]` per event (`onClick`/`onSubmit`/`onLoad`/`onChange`) via the **client-side action runtime** (`PageBuilderPage/runtime/executeAction.ts` + `useActionRunner`) — `runFlow` (`POST /api/flows/{id}/execute` `{input}` → optional `awaitResult` poll `GET /api/flows/executions/{id}` to a terminal status, reading the execution id from `data.id`), `navigate`/`openUrl` (router / `window`, **URL scheme allow-list `assertSafeUrl` blocks `javascript:`/`data:`** — `runtime/urlSafety.ts`), `createRecord`/`updateRecord` (JSON:API `POST`/`PATCH`, Cerbos + write-FLS enforced server-side), `refreshData` (invalidate the named data-source query), `setVar` (page-variable store), `showToast` (sonner); actions run **sequentially** and the first reject **stops the chain** + surfaces one error toast; action values may be `{$bind}` resolved client-side against the event scope (2d); the `button`/`form` built-ins fire events **only in `mode:'runtime'`** (the editor preview is inert); `inspector/fields/EventListField` authors them (add/remove/reorder + per-action sub-forms). **2e owns the post-deploy `e2e-tests/tests/page-builder-v2.spec.ts`** (asserts a persisted record from a button event, not just render); **typed form widgets (slice 2f)**: the `form` widget renders via `@kelta/components` `ResourceForm` (typed input per `FieldType`, Zod validation, field-level authz, pluggable field-renderer registry wired via `setComponentRegistry` to `LookupSelect`/`MultiPicklistSelect`/native `<select>`/`RichTextEditor` — `widgets/builtins/{forms,formFieldRenderers,registerFormFieldRenderers}.tsx`); standalone Input-category widgets `text-input`/`number-input`/`checkbox`/`dropdown`/`datepicker`/`lookup`/`multi-picklist`/`rich-text` (`widgets/builtins/inputs/*`) map a `FieldType`→control, sourcing picklist options from `GET /api/picklist-values?filter[…]` (FIELD/GLOBAL via `fieldTypeConfig.globalPicklistId`, shared `usePicklistOptions`) and reference options from the target collection (`useLookupOptions`→`useLookupDisplayMap`); the old text-only `FormCreate` body is removed; **rich-text/HTML display is sanitized through the same `FieldRenderer` `rich_text` (`stripHtml`) path — never `dangerouslySetInnerHTML` on bound HTML**; submit stays on the authorized JSON:API path (Cerbos/write-FLS server-side, worker validates required/type/unique); client (Zod) validation is advisory only. New `builder.{form,input}.*` i18n keys | per-page Cerbos authz (published+active+tenant is the visibility gate); positive Playwright e2e once deployed |
| External-entity storage (connectors) | **Dispatch seam (Rec 4 slice 4a)**: `StorageAdapter` gained `default String storageType()`; `DispatchingStorageAdapter` (`@Primary`, runtime-core) routes every storage op to the adapter backing the target collection via `storageConfig().adapterConfig()["adapterType"]` (absent/unknown → `PhysicalTableStorageAdapter`; fail-safe fallback never silently mis-routes); external backends implement the `ExternalStorageAdapter` marker + override `storageType()`. Zero behavior change today (no external adapter registered). **REST adapter (slice 4b)**: `ExternalRestStorageAdapter` (`ExternalStorageAdapter`, `storageType=external-rest`) maps query/getById/create/update/delete to GET/POST/PUT/DELETE on `baseUrl+path[+/id]` over a tiny injectable `RestExecutor` seam (fake in tests, real `RestClient` impl deferred to wiring); pushes down pagination + sort + `EQ` filters as query params (best-effort), maps `idAttribute`→`id`, 404→empty, optional bearer token; schema ops no-op, `aggregate` unsupported. 13 unit tests (fake executor). **JDBC adapter (slice 4c)**: `ExternalJdbcStorageAdapter` (`storageType=external-jdbc`) maps CRUD/query to SQL over a foreign table via an injectable `ExternalJdbcConnectionProvider` seam (config: `jdbcUrl`/`username`/`password`/`table`/`idColumn`); pagination→LIMIT/OFFSET, sort→ORDER BY, `EQ`→`WHERE col=?`; values bound as params + every identifier (table/column/sort/filter field) validated against `^[A-Za-z_][A-Za-z0-9_]*$` before interpolation (SQL-injection-safe); `idColumn`→`id`; schema ops no-op, `aggregate` unsupported. 11 unit tests against real H2. Full runtime-core suite green. **Config persistence (slice 4d-1)**: `collection.adapter_config` JSONB column (V144) + `adapterConfig` modeled on the `collections` system collection (`SystemCollectionDefinitions`, JSON field → generic CRUD/MCP write path) + `CollectionLifecycleManager.buildDefinitionFromDb` parses it into `StorageConfig.adapterConfig` (malformed/empty → safe physical-table default). So an external `adapterType` now persists + loads + reaches the dispatcher. **Credential resolution (slice 4d-2a)**: new runtime-core `CredentialProvider` SPI (`resolve(credentialRef) → Optional<ResolvedCredential>`); both external adapters now resolve `adapterConfig.credentialRef` from the credential vault — REST applies auth by cred type (bearer/basic/api_key/oauth2, mirroring `CallApiActionHandler.applyAuth`), JDBC takes basic_auth username/password — so **secrets never live plaintext in `adapter_config`** (inline `bearerToken`/`password` remain a fallback for tests). 6 new tests. **Activation (slice 4d-2b)**: worker beans wired (`kelta-worker/.../service/storage/`) — `RestClientExecutor` (`RestClient`, no-throw on any status), `PooledExternalJdbcConnectionProvider` (HikariCP, one pool per `jdbcUrl+user`), `VaultCredentialProvider` (`CredentialProvider`→`CredentialResolver` via `TenantContext`, empty when vault/tenant/cred absent); both adapters registered as `@Bean`s in `StorageConfig` so `DispatchingStorageAdapter` collects them. **A collection with `adapterType=external-rest`/`external-jdbc` + `adapterConfig` now routes to the external backend end-to-end.** 7 worker tests. **Admin UI (slice 4e)**: `DataSourceSettings` component + a **Data Source** tab on `CollectionDetailPage` (user collections only) — pick physical / external-rest / external-jdbc, fill connection fields + a vault `credentialRef`, save → PATCHes the collection's `adapterConfig`; 5 Vitest. EN i18n (other locales fall back). **Virtual collections from OpenAPI (slice 4f)**: `OpenApiCollectionMapper` derives a flat collection shape from an imported operation's response schema (picks the 2xx JSON schema, unwraps a bare array or array-valued wrapper property → infers `dataPath`, maps OpenAPI type/format → `FieldType`, infers `idAttribute`); `ExternalEntityMaterializer` creates an external-rest collection + a field per derived property via `queryEngine.create` on the `collections`/`fields` system collections (the standard write path — config NATS broadcast + lifecycle init fire; external collections run NO physical-table DDL). `POST /api/api-specs/{specId}/operations/{opId}/materialize` (GET ops only) + the `materialize_api_collection` admin MCP tool. 11 worker unit tests + `ExternalCollectionMaterializeScenarioTest` (real Postgres: import → materialize → collection persisted/listed) | connector packs (Salesforce/Slack) over the module SPI; REST adapter has no response caching/rate-limit yet; 4e/4f Playwright e2e follows post-deploy (live-env constraint) |
| Offline data sync (PWA) | Frontend replica layer in `kelta-ui/app/src/offline/`: `IndexedDbOfflineStore` (IndexedDB via `idb` — records keyed `[collection,id]`, an outbox, and per-collection sync cursors; `InMemoryOfflineStore` for tests/SSR), `SyncEngine` (push outbox FIFO with optimistic temp-id swap on create + 409/4xx drop vs network-5xx stop-and-retry; pull deletions via `GET /api/{collection}/_changes?since=` [Rec 2B-1, #1066] then upserts via the existing `filter[updatedAt][gt]` JSON:API path), `resolveConflict` (server-wins / client-wins / last-write-wins), `useOnlineStatus`. A single server-clock `_changes` cursor is the canonical watermark for both queries; upserts reuse the authorized JSON:API path so Cerbos/FLS are enforced server-side. 27 Vitest tests (incl. `fake-indexeddb` contract tests) | not yet wired into `EndUserShell`/`CustomPage` — the module is standalone + tested; shell integration + a Playwright offline-read/reconnect e2e follow once the PWA shell (#1065) deploys |
| PWA / offline (Rec 2) | **2A installable PWA**: `vite-plugin-pwa` (autoUpdate) on `kelta-ui/app` — web manifest + service worker precaching the app shell (Workbox), SPA navigate-fallback with `/api`/`/control`/`/internal`/`/actuator`/`/openapi` denylisted; API stays network-only. **2C native shell scaffold**: `capacitor.config.json` (`appId io.kelta.app`, `webDir dist`) + `cap:*` npm scripts + `kelta-ui/app/NATIVE.md` build guide wrap the PWA bundle in a Capacitor native WebView. The Capacitor toolchain is dev-installed (not a committed dep — keeps it out of the web CI/lockfile); the generated `ios/`/`android/` projects are `.gitignore`d and built on a native toolchain, not in CI. Native push reuses the existing `PushProvider` SPI + `FcmPushProvider` | **2B offline data**: `deleted_at` tombstones (delete path is hard-delete today), `GET /api/{collection}/_changes?since=` cursor, IndexedDB replica + outbox + conflict resolution. PNG maskable icons (SVG-only today); app-bundle code-split (single ~4 MB chunk). **2C**: native build can't run in CI (toolchain-bound); APNs delivery provider (FCM done); `@capacitor/push-notifications` device-token registration |
| Governed AI agents | **Definition model + CRUD** (`AgentDefinition` → `ai_agent`, kelta-ai V4; `AgentService`/`AgentDefinitionRepository`/`AgentController` at `/api/ai/agents`). **Orchestration runtime**: `AgentRuntimeService` runs a bounded tool-use loop (`AgentModelClient` seam over `AnthropicService` attaches only the agent's allowed tool subset + model/maxTokens overrides; `ToolDispatcher` executes; non-permitted tools refused as defense-in-depth) with per-run iteration (8) + token (100k) caps, monthly-quota check + `recordUsage` per turn; `POST /api/ai/agents/{id}/run` returns final text + tool-call trace. **Audit + authz**: every run (incl. refusals) is persisted to `ai_agent_execution` (kelta-ai V5; tool-call trace + tokens + status) via `AgentExecutionService`, listable at `GET /api/ai/agents/{id}/executions`; runs require `X-User-Id` so tools execute as the invoking user — the worker's per-record Cerbos/FLS advice then applies downstream (no privilege escalation). **PII masking**: `PiiMaskingService` redacts email/SSN/card/phone from tool results before they reach the model or the audit trail (always-on; user input is not masked). **Admin UI**: `AiAgentsPage` (`/ai-agents`, Setup → Automation) — list/create/edit/delete + a run panel showing final text + tool trace + tokens; Vitest-covered (Playwright e2e follows once the page is deployed — the e2e suite runs against the live env) | — (stretch: `MCP_CALL` flow action, hybrid tsvector+vector ranking) |
| Semantic search / RAG | `EmbeddingService` SPI + dependency-free `HashingEmbeddingService` default (overridable via `@ConditionalOnMissingBean`); `VECTOR` field maps to `vector(N)`; **pgvector enabled** (lazy `CREATE EXTENSION` on first VECTOR column; `?::vector` writes; pgvector image for local/CI — prod Postgres needs the extension installed); `StorageAdapter.semanticSearch` cosine (`<=>`) query + `SemanticSearchService` + `POST /api/{collection}/semantic-search` (FLS-respecting; distances in `meta`); **HNSW cosine index auto-emitted**; `semantic_search` MCP user tool; **embed-on-write**: `EmbeddingOnWriteHook` (wildcard before-save) auto-populates a VECTOR field from a configured `embeddingSource` text field via the `EmbeddingService` (dimension-guarded; explicit vectors and untouched source skipped); end-to-end verified against real pgvector (`PhysicalTableStorageAdapterVectorIntegrationTest`) | governed agent runtime (next Rec 3 slice) |

## 🔴 UI Complete, backend MISSING (do not assume these work end-to-end)

| Capability | Built (UI) | Not built (backend) |
|------------|-----------|---------------------|
| Reports & dashboards | Multi-step report builder + dashboard editor | No query-execution engine reading `columns`/`filters`/`groupings`; no chart-data endpoint; no CSV/PDF export |
| Approval processes | Process defs, steps, assignees, field updates | No submit/approve/reject endpoints; no record locking; no `SUBMIT_FOR_APPROVAL` action handler |
| Bulk data operations | INSERT/UPDATE/UPSERT/DELETE job UI + progress | No worker batch processor; no file upload/download for payloads |
| Configuration packages | Export/import wizard, dry-run preview, history | **No `PackageController`/`PackageService`** — UI calls `/api/packages/*` which 404 |
| Record types | Data model + Record Type editor | No runtime enforcement of picklist restriction or layout association |

## ⚪ Planned / enterprise gaps (not started)

SCIM 2.0 provisioning · HA/failover docs (RTO/RPO) · scheduled data export & backup ·
sandbox environments + metadata promotion · data masking · IP allowlisting per tenant ·
delegated administration.

---

This map is distilled from the engineering capability inventory. When you implement or
remove a feature, move its row to the correct section in the same PR so the next agent
trusts it.
