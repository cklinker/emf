---
# Dev allow-all: collection resources (gateway route authorization)
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "collection"
  rules:
    - actions: ["*"]
      effect: EFFECT_ALLOW
      roles: ["*"]
