---
# Dev allow-all: field resources (worker field-level authorization)
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "field"
  rules:
    - actions: ["*"]
      effect: EFFECT_ALLOW
      roles: ["*"]
