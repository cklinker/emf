---
# Dev allow-all: record resources (worker record-level authorization)
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: "default"
  resource: "record"
  rules:
    - actions: ["*"]
      effect: EFFECT_ALLOW
      roles: ["*"]
