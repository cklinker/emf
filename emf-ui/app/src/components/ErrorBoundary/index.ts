/**
 * ErrorBoundary Component Export
 *
 * Error boundary that catches JavaScript errors in child components
 * and displays a fallback UI with recovery options.
 */
export { ErrorBoundary, ErrorFallback, default } from './ErrorBoundary'
export type { ErrorBoundaryProps, ErrorBoundaryState, ErrorFallbackProps } from './ErrorBoundary'
