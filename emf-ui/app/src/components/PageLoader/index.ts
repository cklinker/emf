/**
 * PageLoader Component
 *
 * Exports the PageLoader, Skeleton, and ContentLoader components
 * for consistent loading states across the application.
 */

export {
  PageLoader,
  Skeleton,
  ContentLoader,
  default,
} from './PageLoader';

export type {
  PageLoaderProps,
  SkeletonProps,
  SkeletonVariant,
  ContentLoaderProps,
} from './PageLoader';
