apiVersion: v1
kind: Service
metadata:
  name: {{ include "emf-worker.fullname" . }}
  labels:
    {{- include "emf-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emf-worker.selectorLabels" . | nindent 4 }}
