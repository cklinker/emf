server:
  port: ${SERVER_PORT:8080}
  shutdown: graceful

spring:
  application:
    name: emf-worker
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/emf_control_plane}
    username: ${SPRING_DATASOURCE_USERNAME:emf}
    password: ${SPRING_DATASOURCE_PASSWORD:emf}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-init-sql: SET app.current_tenant_id = ''
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      group-id: ${emf.worker.id:emf-worker-default}
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  lifecycle:
    timeout-per-shutdown-phase: 30s

emf:
  worker:
    id: ${WORKER_ID:}
  storage:
    mode: PHYSICAL_TABLES
    s3:
      enabled: ${EMF_S3_ENABLED:false}
      endpoint: ${EMF_S3_ENDPOINT:http://localhost:3900}
      public-endpoint: ${EMF_S3_PUBLIC_ENDPOINT:https://s3.rzware.com}
      region: ${EMF_S3_REGION:garage}
      bucket: ${EMF_S3_BUCKET:emf-attachments}
      access-key: ${EMF_S3_ACCESS_KEY:}
      secret-key: ${EMF_S3_SECRET_KEY:}
      presigned-url-expiry-minutes: ${EMF_S3_URL_EXPIRY_MINUTES:15}
  kafka:
    topics:
      collection-changed: emf.config.collection.changed

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
    prometheus:
      enabled: true
    metrics:
      enabled: true
  health:
    readinessState:
      enabled: true
    livenessState:
      enabled: true
  metrics:
    distribution:
      percentiles-histogram:
        emf_worker_request_duration_seconds: true
      percentiles:
        emf_worker_request_duration_seconds: 0.5,0.95,0.99
    tags:
      application: ${spring.application.name}

logging:
  level:
    com.emf: ${LOGGING_LEVEL_COM_EMF:INFO}
    root: ${LOGGING_LEVEL_ROOT:INFO}
