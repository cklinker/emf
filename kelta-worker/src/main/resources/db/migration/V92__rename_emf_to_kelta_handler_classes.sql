-- V92: Rename emf to kelta handler classes (no-op)
-- The workflow_action_type table was dropped in V72__drop_legacy_workflow_tables.sql,
-- so there is nothing to update. This migration is kept as a no-op placeholder
-- since V92 was already referenced in the Flyway schema history.
SELECT 1;
